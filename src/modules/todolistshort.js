class TodoListShort {
  constructor(title, desc, prio, date) {
    this.title = title;
    this.desc = desc;
    this.prio = prio;
    this.date = date;
  }

  add() {
    //add new item to list
  }

  edit() {
    //edit one of the current list items
  }

  remove() {
    //remove item from list
  } 

  get() {
    //get information from list item for display
  }

  markAsComplete() {
    //mark item as complete
  }

}